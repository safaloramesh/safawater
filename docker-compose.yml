version: '3.8'
services:
  safa-water:
    build: .
    container_name: safa_water_system
    ports:
      - "8080:3000"
    volumes:
      - safa_permanent_storage:/app/storage
    restart: always

volumes:
  safa_permanent_storage:
